<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="@drawable/widget_bg" android:padding="14dp">

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:gravity="center_vertical">
        <TextView android:layout_width="0dp" android:layout_weight="1"
            android:layout_height="wrap_content" android:text="یادداشت"
            android:fontFamily="@font/lalezar" android:textSize="18sp" android:textColor="#B8770E" />
        <Button android:id="@+id/widget_new" android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:minHeight="0dp"
            android:paddingHorizontal="12dp" android:paddingVertical="4dp"
            android:text="+ جدید" android:textSize="12sp"
            android:background="@drawable/widget_new_bg" android:textColor="#22302B" />
    </LinearLayout>

    <TextView android:id="@+id/widget_title" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:layout_marginTop="10dp"
        android:fontFamily="@font/lalezar" android:textSize="16sp" android:textColor="#22302B"
        android:maxLines="1" android:ellipsize="end" />

    <TextView android:id="@+id/widget_body" android:layout_width="match_parent"
        android:layout_height="0dp" android:layout_weight="1" android:layout_marginTop="4dp"
        android:fontFamily="@font/vazirmatn" android:textSize="12sp" android:textColor="#5C6B63"
        android:maxLines="4" android:ellipsize="end" android:lineSpacingExtra="2dp" />
</LinearLayout>